channels:
  - conda-forge
  - defaults
#   - pytorch

dependencies:
# minimal dependencies for the competition
  - python=3.8
  - scip=7.0.3
  - ecole=0.7.3
  - numpy
  - pyscipopt
  - nb_conda

# additional conda dependencies go here
#   - torch
#   - tensorflow
#   - scikit-learn

# additional pip dependencies go here
#  - pip
#  - pip:
#      - -r tmp/requirements.txt

# Please do not hesitate to send us also a snapshot of your final
# environments to help us troubleshoot potential installation issues:
# conda env export --no-builds
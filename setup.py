import setuptools

setuptools.setup(name="retro_branching",
                 version="0.0.1",
                 description="Combining exact combinatorial optimisation (i.e. branch & bound solvers) with reinforcement learning.",
                 packages=setuptools.find_packages(),
                 python_requires='>=3.7')

